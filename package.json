{
  "name": "cua-constraint-agent",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "experiment": "node --import tsx scripts/experiment.ts",
    "build": "tsc --noEmit",
    "test": "node --import tsx --test --test-concurrency=1 tests/*.test.ts",
    "test:integration": "node --import tsx --test --test-concurrency=1 tests/integration/*.test.ts",
    "typecheck": "tsc --noEmit",
    "format": "prettier --write src scripts tests config package.json tsconfig.json README.md docs/SETUP.md docs/EXPERIMENT.md docs/LLM-BENCHMARK.md docs/STATUS.md",
    "format:check": "prettier --check src scripts tests config package.json tsconfig.json README.md docs/SETUP.md docs/EXPERIMENT.md docs/LLM-BENCHMARK.md docs/STATUS.md",
    "preview": "node --import tsx scripts/preview.ts"
  },
  "dependencies": {
    "playwright": "^1.58.0",
    "yaml": "^2.8.2",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "prettier": "3.6.2",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
